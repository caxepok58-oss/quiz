BOT_TOKEN=123456:your-telegram-bot-token
ADMIN_IDS=123456789
CITY_NAME=Пенза
TZ_NAME=Europe/Moscow
LOOKAHEAD_DAYS=30
SCRAPE_HOUR=6
DB_PATH=quizzes.db

# Only needed if api.telegram.org is blocked on your network (e.g. by an ISP).
# Point this at a local proxy exposed by your VPN client, e.g. Happ's SOCKS5/HTTP
# inbound (leave empty to connect directly, which is the default/normal case).
# BOT_PROXY_URL=http://127.0.0.1:10809
# BOT_PROXY_URL=socks5://127.0.0.1:10808
